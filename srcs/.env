# DATABASE
DB_NAME=inception
DB_ADMIN=wpuser
DB_PASS=wppass
DB_HOST=mariadb-data

# WP / ADMIN
WP_TITLE=FORTY2PORTALS
WP_URL=ookamonu.42.fr
WP_ADMIN=suser
WP_ADMIN_PASS=42bkk
WP_ADMIN_EMAIL=suser@42.fr
# WP_ADMIN_URL=https://ookamonu.42.fr/wp-admin/

# WP USER
WP_USER=user
WP_USER_EMAIL=user@42.fr
WP_USER_PASS=419


#mysql -u luis -p
#docker exec -it mariadb-data bash
#SHOW DATABASES;
#mysql -u root  
#USE inception;
#SHOW TABLES;
# docker stop $(docker ps -qa); docker rm $(docker ps -qa); docker rmi -f $(docker images -qa); docker volume rm $(docker volume ls -q); docker network rm $(docker network ls -q) 2>/dev/null
